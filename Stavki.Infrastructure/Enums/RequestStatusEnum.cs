namespace Stavki.Infrastructure.Enums
{
    public enum RequestStatus
    {
        Created = 0,
        InProgress = 1,
        Done = 2,
    }

    public enum DataSourceType
    {
        Employee = 0,
        Client = 1,
    }
}
